export interface CreateSchedulingData {
    userUuid: string;
    serviceUuid: string;
    date: Date
}

export interface UpdateSchedulingData {
    userUuid: string;
    serviceUuid: string;
    date: Date
}